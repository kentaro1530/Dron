\begin{frame}{To summarize my analysis}
  \begin{itemize}
  \item Items for cross section calculation\\
    $\rightarrow$ List up for parameters.\\
    $\rightarrow$ Check in ditail for CDC efficiency.
  \item \sout{ $d(K^-, n)"X"$ shift in MC}\\
    due to treatment of forward neutron analysis.
  \item Detector resolutions\\
    CDC $\rightarrow$ $K^0$ peak. $\rightarrow$ Estimated by Run68 MC sim. \\
    NC $\rightarrow$ $d(K^-, n \pi^+ \pi^-)"X"$, $d(K^-, n \pi^+)"X"$, $d(K^-, n \pi^-)"X"$.\\
    $\sigma 150ps \sim 200ps$ $\sigma =160ps, 170ps$ files was made, \\
    fitting was not finisheed. 
  \item Background from $d(K^-, n \pi^- \pi^+)"X"$ tails\\
    $\rightarrow$ Systematic study for $d(K^-, n \pi^+ \pi^-)"X"$ gate.\\
    $\sigma=3.0$(almost same default), $\sigma=2.5$, $\sigma=2.0$ analysis was finished.
  \item Summarize my analysis.\\
    $\rightarrow$ NC performance check (run and seg dependece).
  \end{itemize}
\end{frame}


%% \begin{frame}{To do list}
%%   \begin{itemize}
%%   \item MC of $d(K^-, n)"X"$ mass shift\\
%%     $\rightarrow$ To comparison $\gamma$ peak with data and MC.\\
%%     $\rightarrow$ To evaluate intrinsic resolution NC using $d(K^-, n \pi^+ \pi^-)"X"$ peak.
%%   \item CDC efficiency in RUN78.\\
%%     CDC layer1 was used as trigger counter (IH).\\
%%     $\rightarrow$ Today theme.
    
%%   \item To make my analysis summary. 
%%   \end{itemize}
%% \end{frame}
